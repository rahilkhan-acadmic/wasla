requests>=2.28
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
xgboost>=2.0
vaderSentiment>=3.3
yfinance>=0.2.40   # needed only for src/nse_client.py (Indian market data)
fastapi>=0.110
uvicorn[standard]>=0.29   # only needed for local/ECS/App Runner serving, not Lambda
mangum>=0.17              # only needed for the Lambda deployment path
mcp[cli]>=2.0             # only needed for src/mcp_server.py (Claude/MCP client integration)

# Optional, for real (recommended) FinBERT sentiment instead of the offline
# VADER fallback:
# transformers>=4.40
# torch>=2.2
