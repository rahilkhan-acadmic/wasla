requests==2.33.1
numpy==2.4.4
pandas==3.0.2
scikit-learn==1.8.0
xgboost==3.4.1
vaderSentiment==3.3.2
yfinance==1.7.0   # needed only for src/nse_client.py (Indian market data)
fastapi==0.141.1
uvicorn[standard]==0.53.0   # only needed for local/ECS/App Runner serving, not Lambda
mangum==0.22.0              # only needed for the Lambda deployment path
mcp[cli]==2.2.0             # only needed for src/mcp_server.py (Claude/MCP client integration)

# Optional, for real (recommended) FinBERT sentiment instead of the offline
# VADER fallback:
# transformers>=4.40
# torch>=2.2
